# MewCode runtime dependencies. Add new deps here as the project grows.
anthropic>=0.40
openai>=1.40
pyyaml>=6.0
pydantic>=2.0

# ch07: MCP client (official SDK + Streamable HTTP transport)
mcp>=1.0
httpx>=0.27

# ch08: pre-request token estimation for proactive compaction
tiktoken>=0.7

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
